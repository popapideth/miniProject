spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:5432/miniProject
    username: postgres
    password: pop0864730451
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB
